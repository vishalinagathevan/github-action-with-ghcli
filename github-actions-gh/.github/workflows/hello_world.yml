  name: Hello world workflow

  on:
    push:
      branches:
        -main
    pull_request:
      branches:
        -main
    workflow_dispatch:

  jobs:
    hello:
      runs-on: Ubuntu-latest
      steps:
        -uses: actions/checkout@v3
        -name: hello world
         run: echo "hello world"
         shall: bash
    

    googbye:
      runs-on: ubuntu-latest
      steps: 
      -name: goodbye
       run: echo "gooodbye"
       shell: bash  
